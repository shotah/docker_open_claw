# Credential files pushed only by `make <name>-sync` / `remote sync-secret <name>`.
# NOT included in remote-deploy / deploy-manifest.txt (avoids clobbering a good
# server session with a stale laptop copy).
#
# Format: one "name|relative/path" per line. Path may be a file or a directory
# (directories are copied recursively). Blank lines and # comments ignored.

garmin|secrets/garmin/session.json
strava|secrets/strava/tokens.json
ytmusic|secrets/ytmusic/headers.json
google|secrets/google-mcp/credentials
google|secrets/google/credentials.json
google|secrets/google/client_secret.json
